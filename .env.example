DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DB_NAME"
JWT_SECRET="change_this_secret"
JWT_EXPIRES_IN="7d"
# Port the API listens on (frontend dev server uses 3000)
PORT="4000"
# Allowed origin for browser requests (the frontend URL)
CORS_ORIGIN="http://localhost:3000"
